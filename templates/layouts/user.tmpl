{{define "base"}}
{{template "header" .}}
<div id="flashDiv" class="flash">
    <div id="flashText" data-flashtype="{{.flashMessageType}}">{{.flashMessage}}</div>
</div>
{{template "content" .}}
{{template "footer" .}}
{{end}}
